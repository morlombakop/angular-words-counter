export class RandomText { // Should it be an interface
  id: string;
  type: string;
  format: string;
  number: number;
  // tslint:disable-next-line:variable-name
  number_max: 40;
  amount: number;
  time: string;
   // tslint:disable-next-line:variable-name
  text_out: string;
}
